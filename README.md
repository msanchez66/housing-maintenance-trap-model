# housing-maintenance-trap-model
This repository contains the Jupiter Notebook with the model used in the paper "The housing maintenance trap under global demographic decline". It is implemented in Python 3.11 and organized as a Jupyter notebook (GPM-NINT-V30). The full code, includes all scenario implementations, parameters, and figure-generation scripts
